import {NextResponse} from 'next/server';
import {createClient} from '@supabase/supabase-js';
import {createHash,randomUUID} from 'crypto';
const admin=()=>createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.SUPABASE_SERVICE_ROLE_KEY!,{auth:{autoRefreshToken:false,persistSession:false}});
const clean=(v:any)=>String(v??'').trim();
const hash=(v:string)=>createHash('sha256').update(clean(v).replace(/[^0-9A-Za-z]/g,'').toLowerCase()).digest('hex');
export async function POST(req:Request){try{const b=await req.json();const required=['full_name','father_name','dob','gender','cnic','guardian_cnic','address','city','admission_session','class_id','emergency_name','emergency_phone','relationship','password'];for(const k of required)if(!clean(b[k]))return NextResponse.json({error:`${k.replaceAll('_',' ')} is required.`},{status:400});if(clean(b.password).length<8)return NextResponse.json({error:'Password must be at least 8 characters.'},{status:400});const s=admin();const cnicHash=hash(b.cnic);const {data:existing}=await s.from('access_requests').select('id,status').eq('cnic_hash',cnicHash).maybeSingle();if(existing)return NextResponse.json({error:`A registration already exists for this CNIC (${existing.status}).`},{status:409});const requestId=randomUUID();const internalEmail=`student-${requestId}@npsd.invalid`;const {data:u,error:ue}=await s.auth.admin.createUser({email:internalEmail,password:clean(b.password),email_confirm:true,user_metadata:{full_name:clean(b.full_name),role:'student'}});if(ue||!u.user)throw ue||new Error('Unable to create secure account.');const token=randomUUID()+randomUUID();const payload={auth_user_id:u.user.id,tracking_token:token,full_name:clean(b.full_name),father_name:clean(b.father_name),guardian_name:clean(b.guardian_name),dob:b.dob,gender:clean(b.gender),cnic_hash:cnicHash,guardian_cnic_hash:hash(b.guardian_cnic),phone:clean(b.phone)||null,whatsapp:clean(b.whatsapp)||null,email:clean(b.email)||null,address:clean(b.address),city:clean(b.city),admission_session:clean(b.admission_session),class_id:b.class_id,section:clean(b.section)||null,previous_school:clean(b.previous_school)||null,previous_class:clean(b.previous_class)||null,admission_date:b.admission_date||null,photo_url:clean(b.photo_url)||null,emergency_name:clean(b.emergency_name),emergency_phone:clean(b.emergency_phone),relationship:clean(b.relationship),notes:clean(b.notes)||null,status:'pending'};const {data:r,error:re}=await s.from('access_requests').insert(payload).select('id,student_id,tracking_token').single();if(re){await s.auth.admin.deleteUser(u.user.id);throw re}await s.from('profiles').upsert({id:u.user.id,full_name:clean(b.full_name),email:clean(b.email)||internalEmail,role:'student',approved:false},{onConflict:'id'});return NextResponse.json({request_id:r.id,student_id:r.student_id,tracking_token:r.tracking_token});}catch(e:any){return NextResponse.json({error:e?.message||'Registration failed.'},{status:500})}}
