'use client';
import {useEffect,useMemo,useState} from 'react';
import {QRCodeSVG} from 'qrcode.react';
import {supabase} from '../lib/supabase';

type Child={id:string;student_id:string;full_name:string;father_name?:string|null;photo_url?:string|null;class_id?:string|null;className?:string};
const empty={full_name:'',father_name:'',guardian_name:'',dob:'',gender:'',cnic:'',guardian_cnic:'',phone:'',whatsapp:'',email:'',address:'',city:'Dadu',admission_session:'2026-27',class_id:'',section:'',previous_school:'',previous_class:'',admission_date:'',photo_url:'',emergency_name:'',emergency_phone:'',relationship:'',notes:'',password:'',confirm_password:''};
export default function Home(){const [view,setView]=useState<'home'|'register'|'status'|'login'|'portal'>('home');const [session,setSession]=useState<any>(null);const [checking,setChecking]=useState(true);useEffect(()=>{const s=supabase();s.auth.getUser().then(async({data})=>{if(data.user){const {data:p}=await s.from('profiles').select('approved,role,full_name').eq('id',data.user.id).maybeSingle();if(p?.approved&&p.role === 'student'){setSession({...data.user,role:p.role,full_name:p.full_name});setView('portal')}}setChecking(false)});const {data:l}=s.auth.onAuthStateChange(()=>{});return()=>l.subscription.unsubscribe()},[]);if(checking)return <main className="shell"><div className="loaderCard">Loading NPSD Portal…</div></main>;if(view==='portal'&&session)return <Dashboard user={session}/>;return <main className="shell">{view==='home'&&<HomeView go={setView}/>} {view==='register'&&<Registration go={setView}/>} {view==='status'&&<Status go={setView}/>} {view==='login'&&<Login onSuccess={(u:any)=>{setSession(u);setView('portal')}} go={setView}/>}</main>}
function HomeView({go}:{go:any}){return <div className="hero"><div className="heroCard"><div className="logo">NPSD</div><span className="eyebrow">Noble Public School Dadu</span><h1>Student Portal</h1><p>Apply for portal access, track your admission request, and securely access your school records after approval.</p><div className="entryGrid"><button className="entryCard" onClick={()=>go('login')}><span>🎓</span><b>Existing / Enrolled Student</b><small>Already enrolled? Open your Student Portal.</small><strong>Login →</strong></button><button className="entryCard" onClick={()=>go('register')}><span>📝</span><b>New Admission</b><small>Apply for portal access and track your application.</small><strong>Apply Now →</strong></button></div><button className="statusLink" onClick={()=>go('status')}>Already applied? Check admission status →</button><div className="steps"><span>01 Registration</span><span>02 Admin Approval</span><span>03 CNIC Verification</span><span>04 Portal</span></div></div><div className="featureGrid"><Feature icon="🪪" t="Digital Student Card" d="View and print your official portal card."/><Feature icon="📊" t="Results & Performance" d="See published marks, grades and progress."/><Feature icon="📅" t="Attendance" d="Track daily attendance and percentage."/><Feature icon="🔐" t="Secure Access" d="Approval and row-level data isolation."/></div></div>}
function Feature({icon,t,d}:{icon:string;t:string;d:string}){return <div className="feature"><b>{icon} {t}</b><p>{d}</p></div>}
function Registration({go}:{go:any}){const [form,setForm]=useState<any>(empty);const [classes,setClasses]=useState<any[]>([]);const [step,setStep]=useState(1);const [busy,setBusy]=useState(false);const [error,setError]=useState('');const [done,setDone]=useState<any>(null);useEffect(()=>{fetch('/api/classes',{cache:'no-store'}).then(r=>r.json()).then(j=>setClasses(j.classes||[])).catch(()=>setClasses([]))},[]);const set=(k:string,v:string)=>setForm((x:any)=>({...x,[k]:v}));async function submit(e:any){e.preventDefault();setError('');if(form.password!==form.confirm_password)return setError('Passwords do not match.');setBusy(true);try{const r=await fetch('/api/register',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(form)});const j=await r.json();if(!r.ok)throw new Error(j.error||'Registration failed.');setDone(j);setForm(empty)}catch(err:any){setError(err.message)}finally{setBusy(false)}}if(done)return <div className="centerCard"><div className="successIcon">✓</div><span className="eyebrow">Application Submitted</span><h1>Registration received 🎉</h1><p>Your application is now waiting for admin review.</p><div className="requestBox"><span>Student ID / Roll No</span><strong>{done.student_id}</strong><span>Request ID</span><strong>{done.request_id}</strong><span>Tracking Token</span><strong>{done.tracking_token}</strong></div><p className="muted">Save the Request ID and tracking token. After approval, use your CNIC and password to enter the portal.</p><button className="primary" onClick={()=>go('status')}>Check Request Status</button></div>;
return <div className="formShell"><div className="formTop"><button className="back" onClick={()=>go('home')}>← Back</button><span className="eyebrow">New Student Registration</span><h1>Complete Admission Form</h1><p>Fill in the student details carefully. Required fields are marked *.</p></div><div className="stepper"><Step n="1" t="Student" active={step===1} done={step>1}/><Step n="2" t="Family" active={step===2} done={step>2}/><Step n="3" t="Academic" active={step===3} done={step>3}/><Step n="4" t="Security" active={step===4} done={false}/></div><form onSubmit={submit} className="formCard">{step===1&&<><SectionTitle t="Student Information"/><div className="grid2"><Field l="Full Name *" v={form.full_name} set={set} k="full_name"/><Field l="Date of Birth *" type="date" v={form.dob} set={set} k="dob"/><Select l="Gender *" v={form.gender} set={set} k="gender" opts={['Male','Female','Other']}/><Field l="B-Form / Student CNIC *" v={form.cnic} set={set} k="cnic" placeholder="13 digits" inputMode="numeric"/><Field l="Student Phone" v={form.phone} set={set} k="phone" placeholder="11 digits" inputMode="numeric"/><Field l="WhatsApp" v={form.whatsapp} set={set} k="whatsapp" placeholder="11 digits" inputMode="numeric"/><Field l="Email (optional)" type="email" v={form.email} set={set} k="email"/><Field l="Photo URL (optional)" v={form.photo_url} set={set} k="photo_url"/></div><Field l="Complete Address *" v={form.address} set={set} k="address"/><div className="grid2"><Field l="City *" v={form.city} set={set} k="city"/><Field l="Admission Date" type="date" v={form.admission_date} set={set} k="admission_date"/></div></>}{step===2&&<><SectionTitle t="Family / Guardian Information"/><div className="grid2"><Field l="Father Name *" v={form.father_name} set={set} k="father_name"/><Field l="Guardian Name" v={form.guardian_name} set={set} k="guardian_name"/><Field l="Father / Guardian CNIC *" v={form.guardian_cnic} set={set} k="guardian_cnic" placeholder="13 digits" inputMode="numeric"/><Field l="Relationship *" v={form.relationship} set={set} k="relationship" placeholder="Father / Mother / Guardian"/><Field l="Emergency Contact Name *" v={form.emergency_name} set={set} k="emergency_name"/><Field l="Emergency Phone *" v={form.emergency_phone} set={set} k="emergency_phone" placeholder="11 digits" inputMode="numeric"/></div></>}{step===3&&<><SectionTitle t="Academic & Admission"/><div className="grid2"><Field l="Admission Session *" v={form.admission_session} set={set} k="admission_session"/><Select l="Class *" v={form.class_id} set={set} k="class_id" opts={classes.map(c=>({value:c.id,label:`${String(c.name).replace(/^Class\s*/i,'')} — ${c.section}`}))}/><Field l="Section (if applicable)" v={form.section} set={set} k="section"/><Field l="Previous School" v={form.previous_school} set={set} k="previous_school"/><Field l="Previous Class" v={form.previous_class} set={set} k="previous_class"/></div><label>Additional Notes<textarea value={form.notes} onChange={e=>set('notes',e.target.value)} /></label></>}{step===4&&<><SectionTitle t="Portal Security"/><div className="securityNote">🔐 Your password is protected by Supabase Auth and is not stored in the student profile database.</div><div className="grid2"><Field l="Create Password *" type="password" v={form.password} set={set} k="password"/><Field l="Confirm Password *" type="password" v={form.confirm_password} set={set} k="confirm_password"/></div><div className="summary"><b>Final Review</b><p>{form.full_name||'Student'} • {form.cnic||'CNIC not entered'} • {form.admission_session}</p><p>Class: {classes.find(c=>c.id===form.class_id)?.name?.replace(/^Class\s*/i,'')||'Not selected'} {form.section?`• ${form.section}`:''}</p><p>After submission: <strong>Admin Review → Verification → Approval → CNIC + Password → Portal</strong></p></div></>}{error&&<div className="error">{error}</div>}<div className="formActions">{step>1&&<button type="button" className="secondary" onClick={()=>setStep(step-1)}>← Previous</button>}{step<4?<button type="button" className="primary" onClick={()=>setStep(step+1)}>Continue →</button>:<button className="primary" disabled={busy}>{busy?'Submitting…':'Submit Registration'}</button>}</div></form></div>}
function Step({n,t,active,done}:{n:string;t:string;active:boolean;done:boolean}){return <div className={`step ${active?'active':''} ${done?'done':''}`}><b>{done?'✓':n}</b><span>{t}</span></div>}
function SectionTitle({t}:{t:string}){return <div className="sectionTitle"><h2>{t}</h2></div>}
function Field({l,v,set,k,type='text',placeholder='',inputMode}:any){return <label>{l}<input type={type} value={v} placeholder={placeholder} inputMode={inputMode} required={l.includes('*')} onChange={e=>set(k,e.target.value)} /></label>}
function Select({l,v,set,k,opts}:any){return <label>{l}<select value={v} required={l.includes('*')} onChange={e=>set(k,e.target.value)}><option value="">Select…</option>{opts.map((o:any)=><option key={typeof o==='string'?o:o.value} value={typeof o==='string'?o:o.value}>{typeof o==='string'?o:o.label}</option>)}</select></label>}
function Status({go}:{go:any}){const [id,setId]=useState('');const [token,setToken]=useState('');const [data,setData]=useState<any>(null);const [error,setError]=useState('');async function check(e:any){e.preventDefault();setError('');const r=await fetch('/api/request-status',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({request_id:id,tracking_token:token})});const j=await r.json();if(!r.ok)setError(j.error||'Request not found.');else setData(j)}return <div className="centerCard"><button className="back" onClick={()=>go('home')}>← Back</button><span className="eyebrow">Application Tracker</span><h1>Check Request Status</h1><p>Enter the Request ID and tracking token from your registration confirmation.</p><form onSubmit={check}><Field l="Request ID *" v={id} set={(_:any,v:string)=>setId(v)} k=""/><Field l="Tracking Token *" v={token} set={(_:any,v:string)=>setToken(v)} k=""/>{error&&<div className="error">{error}</div>}<button className="primary full" type="submit">Check Status</button></form>{data&&<div className="statusResult"><span className={`status ${data.status}`}>{data.status}</span><h2>{data.student_id}</h2><p>{data.full_name} • {data.class_name||'Class pending'}</p><p>{data.status==='approved'?'Your portal access is approved. Use CNIC + password to login.':data.status==='rejected'?'Please contact the school administration.':'Your application is still under admin review.'}</p>{data.status==='approved'&&<button className="primary" onClick={()=>go('login')}>Continue to Verification</button>}</div>}</div>}
function Login({onSuccess,go}:{onSuccess:any;go:any}){const [cnic,setCnic]=useState('');const [password,setPassword]=useState('');const [busy,setBusy]=useState(false);const [error,setError]=useState('');async function submit(e:any){e.preventDefault();setBusy(true);setError('');try{const r=await fetch('/api/cnic-login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({cnic,password})});const j=await r.json();if(!r.ok)throw new Error(j.error||'Verification failed.');const s=supabase();const {error:e2}=await s.auth.setSession(j.session);if(e2)throw e2;const {data:u}=await s.auth.getUser();if(!u.user)throw new Error('Unable to verify the signed-in account.');const {data:p}=await s.from('profiles').select('role,full_name,approved').eq('id',u.user.id).maybeSingle();if(!p?.approved||!['student','parent'].includes(p.role))throw new Error('Account is not approved.');onSuccess({...u.user,role:p.role,full_name:p.full_name})}catch(e:any){setError(e.message)}finally{setBusy(false)}}return <div className="centerCard"><button className="back" onClick={()=>go('home')}>← Back</button><span className="eyebrow">Secure Verification</span><h1>Welcome to NPSD</h1><p>Use your Student CNIC / B-Form and password after approval.</p><form onSubmit={submit}><Field l="CNIC / B-Form *" v={cnic} set={(_:any,v:string)=>setCnic(v)} k=""/><Field l="Password *" type="password" v={password} set={(_:any,v:string)=>setPassword(v)} k=""/>{error&&<div className="error">{error}</div>}<button className="primary full" disabled={busy}>{busy?'Verifying…':'Open Portal'}</button></form><p className="muted">Forgot your password? Contact school administration for a secure password reset. Not approved yet? <button className="textBtn" onClick={()=>go('status')}>Check application</button></p></div>}
function Dashboard({user}:{user:any}){const [children,setChildren]=useState<Child[]>([]);const[selected,setSelected]=useState('');const[attendance,setAttendance]=useState<any[]>([]);const[results,setResults]=useState<any[]>([]);const[announcements,setAnnouncements]=useState<any[]>([]);const[loading,setLoading]=useState(true);useEffect(()=>{load()},[user.id]);async function load(){const s=supabase();let list:Child[]=[];if(user.role==='student'){const {data}=await s.from('students').select('id,student_id,full_name,father_name,photo_url,class_id,classes(name,section)').eq('user_id',user.id).maybeSingle();if(data)list=[{...data,className:(data as any).classes?`${String((data as any).classes.name).replace(/^Class\s*/i,'')} / ${(data as any).classes.section}`:''}]}else{const {data:p}=await s.from('parents').select('id').eq('user_id',user.id).maybeSingle();if(p){const {data:links}=await s.from('parent_student_links').select('student_id,students(id,student_id,full_name,father_name,photo_url,class_id,classes(name,section))').eq('parent_id',p.id).eq('approved',true);list=(links||[]).map((x:any)=>({...x.students,className:x.students?.classes?`${String(x.students.classes.name).replace(/^Class\s*/i,'')} / ${x.students.classes.section}`:''})).filter(Boolean)}}setChildren(list);if(list[0]){setSelected(list[0].id);await loadChild(list[0].id)}else setLoading(false)}const loadChild=async(id:string)=>{const s=supabase();const[a,r,n]=await Promise.all([s.from('attendance').select('attendance_date,status').eq('student_id',id).order('attendance_date',{ascending:false}).limit(31),s.from('results').select('marks_obtained,total_marks,grade,position,pass,exams(name,exam_date)').eq('student_id',id).eq('published',true).order('id',{ascending:false}).limit(50),s.from('announcements').select('id,title,body,published_at').eq('published',true).order('published_at',{ascending:false}).limit(5)]);setAttendance(a.data||[]);setResults(r.data||[]);setAnnouncements(n.data||[]);setLoading(false)};const child=children.find(c=>c.id===selected);if(loading)return <main className="shell"><div className="loaderCard">Loading student portal…</div></main>;return <main className={"shell "+(dark?'themeDark':'')}><div className="portalShell"><header className="portalHeader"><div><span className="eyebrow">NPSD Student Portal</span><h1>Welcome, {user.full_name||child?.full_name||'Student'}</h1><p>{child?.student_id} • {child?.className||'Class'}</p></div><div className="navActions"><button className="themeBtn" onClick={()=>setDark(!dark)}>{dark?'☀️':'🌙'} Theme</button><button className="secondary" onClick={()=>supabase().auth.signOut().then(()=>location.reload())}>Sign out</button></div></header><div className="portalGrid"><section className="portalCard"><h2>Student Profile</h2>{child?.photo_url&&<img src={child.photo_url} alt="Student" className="studentPhoto"/>}<p><b>Name:</b> {child?.full_name}</p><p><b>Student ID:</b> {child?.student_id}</p><p><b>Father:</b> {child?.father_name||'—'}</p><p><b>Class:</b> {child?.className||'—'}</p></section><section className="portalCard cardPanel"><h2>Digital Student Card</h2><div className="studentCardPreview printCard"><div className="cardTop"><div><div className="cardLogo">NPSD</div><small>NOBLE PUBLIC SCHOOL DADU</small></div><span>STUDENT ID CARD</span></div><div className="cardBody"><div className="cardPhoto">{child?.photo_url?<img src={child.photo_url} alt="Student"/>:'🎓'}</div><div className="cardInfo"><h2>{child?.full_name}</h2><p><b>Student ID</b><span>{child?.student_id}</span></p><p><b>Class / Section</b><span>{child?.className||'—'}</span></p><p><b>Father</b><span>{child?.father_name||'—'}</span></p><p><b>Status</b><span>ENROLLED</span></p></div><div className="qrBox"><QRCodeSVG value={child?.student_id||''} size={126} level="H" includeMargin/><small>SCAN FOR ATTENDANCE</small></div></div><div className="cardFooter"><span>Official Student Card</span><span>NPSD · 2026–27</span></div></div><button className="primary" onClick={()=>window.print()}>🖨️ Print / Save PDF</button></section><section className="portalCard"><h2>Attendance</h2><p>{attendance.filter(a=>a.status==='present').length} present records in recent history.</p>{attendance.slice(0,7).map((a,i)=><p key={i}>{a.attendance_date} — <b>{a.status}</b></p>)}</section><section className="portalCard"><h2>Results</h2>{results.length?results.slice(0,8).map((r,i)=><p key={i}>{(r.exams as any)?.name||'Exam'} — {r.marks_obtained}/{r.total_marks} — {r.grade||'—'}</p>):<p className="muted">No published results yet.</p>}</section><section className="portalCard"><h2>Announcements</h2>{announcements.length?announcements.map(a=><div key={a.id}><b>{a.title}</b><p>{a.body}</p></div>):<p className="muted">No announcements.</p>}</section></div></div></main>}
